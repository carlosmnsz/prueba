programa{
    var
        entero a, b, c;
        lista logico d, e;
        lista real f;
        real g;
        lista entero h;
        logico i, j, k;
    ;

    procedimiento A (x, y: entero; z, z2 : lista entero);
    inicio
        {
            var
                entero s;
            ;
            x = 1;
            y = 2;
            z = [1, 0, 2, 5];
        }
    fin;

    procedimiento B;
    inicio
        {
            var
                logico hai;
            ;
            salida "HOLA", "MUNDO", h;
            entrada a, b, c;
        }
    fin;

    A(a, b, h, h);
    B();

    f>>;
    f<<;
    $f;


    a = (b + c)/(c * #h);

    si (!i && (a>0)) entonces
        entrada c;
    sino
        entrada b;
    ;

    mientras ((g > -2.1) && (#d==a) && (d@a!=i))
        {
        para a:=b+c hasta_aumento a*10 hacer
        inicio
            b = -c;
        fin;
        f = [3.0, 2.0];
        }
    ;

    
}